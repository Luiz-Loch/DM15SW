import { SafeAreaView, Text } from "react-native";

export default function Plants() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Edit `app/(tabs)/plants.jsx to edit this screen.</Text>
    </SafeAreaView>
  );
}
